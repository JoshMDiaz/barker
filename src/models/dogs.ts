export interface DogModel {
  name: string;
  breed: string;
  gender: string;
  eyes: string;
  fixed: boolean;
  papered: boolean;
  registered: boolean;
  description: string;
  birthdate: string;
  photos: Array<string>;
}
